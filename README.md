  Object Recognition System (OpenCV + KNN, Real-Time Detection)

Современная система распознавания объектов в реальном времени, основанная на Python, OpenCV и KNN-классификаторе.
Проект демонстрирует полный цикл Computer Vision:

подготовка данных → аугментация → обучение модели → распознавание объектов через камеру.

Идеально подходит для портфолио, учебных работ и демонстрации навыков в области искусственного интеллекта.

🚀 Возможности

📷 Распознавание объектов в реальном времени через веб-камеру

🧭 Работа с несколькими классами (book, cup, phone, etc.)

🌀 Аугментация данных (повороты, зеркалирование)

🧼 Фильтрация фона для повышения точности

🧮 Собственный KNN-классификатор

🔍 Отрисовка рамки (bounding box), подписи объекта и confidence (%)

💾 Автоматическое ведение логов в predictions.log

🧹 Очистка логов при каждом запуске

⌨️ Горячие клавиши:

q — выход

m — смена режима работы

s — сохранить текущий кадр

🗂️ Структура проекта
project/
├── data/             # Исходные изображения по классам
├── data_aug/         # Изображения после аугментации
├── main.py           # Основной скрипт: распознавание в реальном времени
├── resize.py         # Подготовка данных + аугментация
├── model.pkl         # Сохранённая модель KNN
├── predictions.log   # Журнал предсказаний
└── README.md

⚙️ Установка

Убедитесь, что установлен Python 3 и pip:

sudo apt install python3 python3-pip
pip install opencv-python scikit-learn numpy joblib


Клонирование проекта:

git clone https://github.com/NURJAKS/AI-Recognition-Project.git
cd AI-Recognition-Project

🧪 Подготовка данных

Добавьте изображения в соответствующие папки:

data/
 ├── book/
 ├── cup/
 ├── phone/
 └── ...


Формат изображений: .jpg или .jpeg.

Запустите подготовку и аугментацию датасета:

python3 resize.py


Будет создана папка data_aug/, содержащая 64×64 grayscale изображения.

▶️ Запуск распознавания
python3 main.py


Система автоматически:

загрузит модель (model.pkl)

активирует веб-камеру

начнёт обработку кадров

отобразит:

bounding box

название класса

confidence %

Если модели нет — она будет обучена автоматически.

🧠 Как работает модель

Используется алгоритм K-Nearest Neighbors (KNN) — простой, быстрый и интерпретируемый ML-метод, отлично подходящий для небольших кастомных датасетов.

Pipeline обработки:

Конвертация изображений → grayscale

Изменение размера до 64×64

Нормализация

Создание аугментированного набора данных

Обучение KNN

Захват изображения с камеры

Классификация и вывод результата на экран

Подход гарантирует хорошую точность даже на ограниченном количестве примеров.

📊 Пример работы

При запуске отображается:

рамка вокруг объекта,

название класса,

уверенность предсказания,

очищенный фон,

плавное обновление в реальном времени.

Работает на любой USB-камере или встроенной веб-камере ноутбука.

📌 Возможные улучшения

Проект легко расширяется и может быть улучшен для продвинутого уровня:

🔥 переход на CNN (TensorFlow / PyTorch)

⚡ ускорение через ONNX Runtime

📦 контейнеризация (Docker)

🌐 веб-интерфейс (Flask + WebRTC)

📊 метрики точности (confusion matrix, accuracy)

💾 сохранение лучших предсказаний

🔄 полноценный ML-пайплайн: сбор данных → обучение → inference

Если нужно — могу подготовить улучшенную версию проекта.

🧑‍💻 Автор

Nurbek Abildaev · 2025
